# Data folder

This folder stores the source recipe dataset used by the recipe pipeline.

- `dataset.csv` is the source table that gets converted into scene-based recipe graphs.
- Generated graph files are written to `../recipe_graphs/v3/`.
- Video clips or observation artifacts can be added later if you want to connect the dynamic scene pipeline to real footage.
